"""
Fine-Tuned Qwen Model Integration
(Used only if GPU allows)

For tomorrow demo:
USE_FINE_TUNED_MODEL = False
"""

def generate_grounded_answer(question, context, lang):
    """
    Placeholder for fine-tuned model generation.
    demo uses retrieval-only mode.
    """

    return context   # fallback
